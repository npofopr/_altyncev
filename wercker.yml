box: debian
build:
  steps:
    - arjen/hugo-build:
      version: "0.74.3"
      theme: hugo-coder
      config: config.toml
      flags: --buildDrafts=false
deploy:
  steps:
    - install-packages:
      packages: git ssh-client
    - uetchy/gh-pages@0.3.1:
      token: $GIT_TOKEN
      domain: altyncev.ru
      basedir: public
